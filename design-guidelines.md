# デザインガイドライン — HTML Slide Starter

## 参考スライドから抽出したデザイン原則

### 原則1: ライトベース + 余白の贅沢使い
背景はウォームホワイト（`#faf9f7`）。スライドの「空気」こそがメッセージを際立たせる。
ライトテーマを選ぶ背景は、昼間のプレゼン・スクリーン印刷への適性と、日本語フォントの可読性にある。
余白はパディング `--sp-xl`（64px）が基本。詰め込みは視認性を損なう。

### 原則2: セクション見出しにドット prefix
コンテンツスライドの `h2` には、左側に青い塗り円（●）が自動付加される。
これはスライドの「位置づけ」を示すビジュアル的アンカーであり、聴衆のナビゲーションを助ける。
CSS が自動付与するため、`.slide-content .body > h2` に特別な記述は不要。

### 原則3: マルチカラーアクセント体系
単一のアクセント色ではなく、5色のパレットで意味を色分けする。

| 色 | 変数 | 意味・用途 |
|----|------|-----------|
| ブルー | `--color-accent` | プライマリ。ドット・リスト・フロー矢印・KPI数字 |
| ピンク | `--color-accent-2` | 課題・問題提起・緊張感 |
| アンバー | `--color-accent-3` | 強調・ハイライト・注意・ラベル |
| グリーン | `--color-accent-4` | 解決・フェーズ1・ポジティブな結果 |
| パープル | `--color-accent-5` | 将来展望・フェーズ3・拡張性 |

色は**テキストと線のみ**に使う。カードやボックスを塗りつぶすと目が疲れる。

### 原則4: グラデーション背景は「特別な瞬間」だけ
`--gradient-pastel`（パステルblob）を使うのは `slide-title`・`slide-statement`・`slide-end` の3種のみ。
コンテンツスライドに使うと希薄化して効果が消える。グラデーションは視線を集める「ここが重要」のシグナル。

### 原則5: バッジ・ハイライトで文中のキーワードを色付け
`Stream A / Stream B`、`Phase 1 / 2 / 3`、`Year 1 / 2 / 3+` のような分類ラベルは `.badge` で表示。
本文中の重要語は `.highlight` でインラインマーキング。どちらも背景は薄い透明色で、テキストの色が意味を担う。

### 原則6: フェーズカードは色で段階を示す
ロードマップ・フェーズ表現は、上線ボーダーカード（`.card-green`、`.card-yellow`、`.card-purple`）で
「いまどこにいるか」を色でも示す。番号と色の二重エンコードで認知負荷を下げる。

---

## 設計思想

「ミニマルで読みやすい」を最優先とする。装飾ではなく、余白・タイポグラフィ・構造でメッセージを伝える。

---

## カラーパレット

| 用途 | 変数 | デフォルト値 | 運用ルール |
|------|------|-------------|-----------|
| ブラウザ背景 | `--color-chrome` | `#1c1c2e` | ダーク。ナビUIの視認性を保つ |
| スライド背景 | `--color-bg` | `#faf9f7` | ウォームホワイト |
| テキスト（メイン） | `--color-fg` | `#1a1a2e` | ディープネイビーブラック |
| テキスト（サブ） | `--color-muted` | `rgba(26,26,46,.5)` | 補足・説明・本文 |
| アクセント（ブルー） | `--color-accent` | `#4f7df7` | プライマリ |
| アクセント（ピンク） | `--color-accent-2` | `#f472b6` | 課題・問題 |
| アクセント（アンバー） | `--color-accent-3` | `#f59e0b` | 強調・注意 |
| アクセント（グリーン） | `--color-accent-4` | `#22c55e` | 解決・ポジティブ |
| アクセント（パープル） | `--color-accent-5` | `#a78bfa` | 将来・拡張 |
| ボーダー | `--color-border` | `rgba(26,26,46,.08)` | 区切り線・カード枠 |
| カード背景 | `--color-card-bg` | `rgba(26,26,46,.025)` | ごく薄い暗色 |

### グラデーション背景
`--gradient-pastel`：ブルー・ピンク・ミント・パープルのパステルblob。
タイトル・ステートメント・エンドスライド専用。

---

## タイポグラフィ

### フォントスタック
- **本文**: Helvetica Neue → Hiragino Sans → Yu Gothic → Noto Sans JP
- **コード・数字**: SF Mono → Fira Code → Consolas

### サイズスケール
| トークン | サイズ | 用途 |
|----------|--------|------|
| `--text-4xl` | 5rem (80px) | タイトルスライドの h1 |
| `--text-3xl` | 3.5rem (56px) | ステートメント・セクション h2 |
| `--text-2xl` | 2.5rem (40px) | コンテンツスライドの h2 |
| `--text-xl` | 1.75rem (28px) | h3・サブ見出し |
| `--text-lg` | 1.25rem (20px) | 本文・リスト項目 |
| `--text-base` | 1rem (16px) | カード本文 |
| `--text-sm` | 0.875rem (14px) | ラベル・日付 |
| `--text-xs` | 0.75rem (12px) | タグ・バッジ |

### ウェイト
- **400 (normal)**: 本文、説明テキスト、`.muted`
- **500 (medium)**: h3、バッジラベル
- **700 (bold)**: h1、h2、強調テキスト

---

## レイアウト原則

### 1スライド1メッセージ
- 伝えたいことは1つに絞る
- 情報が多い場合はスライドを分割する（詰め込まない）

### 余白を恐れない
- パディング: `--sp-xl`（4rem = 64px）が基本
- 要素間: `--sp-lg`（2.5rem = 40px）以上を確保
- 余白 = 読みやすさ

### テキスト量の目安
| スライドタイプ | テキスト上限 |
|---------------|-------------|
| タイトル | h1（1〜2行） + サブ（1〜2行） |
| コンテンツ | h2 + 本文3〜5行 or リスト3〜5項目 |
| ステートメント | 1〜3行の短文（大きく見せる） |
| セクション | h2（1行） + 補足（1行） |
| ランディング | 1〜3行の確信文 |
| エンド | h2（1行） + 名前（1行） |

---

## 図解の使い方

### フローチャート (`flow`)
- **用途**: プロセス、手順、タイムライン
- **ステップ数**: 3〜4個が最適
- **矢印**: `→` を `flow-arrow` で挟む

### カードグリッド (`cols-3` + `card`)
- **用途**: 並列の概念、3つの柱、フェーズ比較
- **カード数**: 3個が標準（`cols-2` で2個も可）
- **フェーズ表現**: `.card-green`（P1）→ `.card-yellow`（P2）→ `.card-purple`（P3）

### 数字ハイライト (`stat`)
- **用途**: KPI、成果、インパクトのある数値
- **数字**: 大きく、モノスペースで、アクセント色

### 2カラム比較 (`cols-2`)
- **用途**: Before/After、旧/新、問題/解決
- **区切り**: 右カラムに `border-left` を付けると明確

### バッジ (`badge`)
- **用途**: Stream A/B、Phase 1/2/3、Year 1/2/3+ などの分類ラベル
- **配置**: カード・ボックスの上部、または見出し付近

### ハイライト (`highlight`)
- **用途**: 本文中のキーワードを色でマーキング
- **原則**: 1スライドに最大2〜3語まで。多用すると効果が消える

---

## テーマカスタマイズ手順

1. `theme/sample.css` の `:root` 変数を変更
2. 変更するのは **変数の値だけ**。セレクタやプロパティは変えない
3. よく変えるもの:
   - `--color-accent`: ブランドカラーに合わせる
   - `--color-bg`: より白にする場合は `#ffffff` に
   - `--font-sans`: ブランドフォントに差し替え

---

## アンチパターン（やってはいけないこと）

- スライドにテキストを詰め込む（5行超は分割する）
- アクセント色を背景の塗りに使う（目が疲れる）
- グラデーション背景をコンテンツスライドに使う（希薄化する）
- 画像を `background-image` だけで指定する（印刷で消える）
- `engine/` のファイルを直接編集する（全デッキに影響）
- 1スライドに複数の論点を混ぜる
- `.highlight` を1スライドに4語以上使う（すべてが埋もれる）
